# Live GRIB2 decoding — ONLY needed to pull fresh NOAA GEFS operational cycles
# (POST /api/ingest/run-cycle) or to rebuild the sample data with
# scripts/fetch_gefs_reforecast_sample.py.
#
# Install on top of the core set:  pip install -r requirements.txt -r requirements-live.txt
#
# Windows note: the `eccodes` wheel bundles its own libeccodes + definition files, but a
# few releases shipped a broken definitions bundle on Windows ("Unable to find boot.def",
# "flex scanner error"). `ecmwflibs` provides a known-good binary + definitions and wires
# the paths automatically, so it is listed first. If `python -m eccodes selfcheck` still
# fails, use conda-forge (`conda install -c conda-forge eccodes python-eccodes`) or WSL.

ecmwflibs>=0.6
eccodes>=2.38
cfgrib>=0.9.10,<0.10
xarray>=2024.1

# Bulk ERA5 observations from the Copernicus CDS
# (scripts/fetch_era5_cds_district_observations.py). Needs a free CDS account and a
# personal access token in ~/.cdsapirc; see that script's docstring. Fetch-side only -
# never imported by the serving app, which must stay inside 512 MB.
cdsapi>=0.7.7
netCDF4>=1.6,<2.0

# IMD gauge-based gridded rainfall, merged into precip_mm on top of a CDS file
# (scripts/fetch_imd_district_rainfall.py). Plain HTTP POST to imdpune.gov.in - no
# account or key needed, unlike CDS above.
imdlib>=0.1.21
